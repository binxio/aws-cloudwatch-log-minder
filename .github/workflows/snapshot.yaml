name: release
on: [push]
#on:
#  push:
#    tags:
#      - '*'
jobs:
  build:
    name: snapshot
    runs-on: ubuntu-latest
    steps:
      - run: pwd
        id: step1
      - run: ls
        id: step2
      - run: make snapshot
        id: step3
