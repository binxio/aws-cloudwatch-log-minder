name: release
on: [push]
#on:
#  push:
#    tags:
#      - '*'
jobs:
  build:
    name: snapshot
    runs-on: ubuntu-latest
    steps:
      - run: pwd
        id: step1
      - run: find . -type f -print
        id: step2
      - run: make snapshot
        id: step3
