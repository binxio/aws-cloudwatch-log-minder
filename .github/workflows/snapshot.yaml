name: release
on: [push]
#on:
#  push:
#    tags:
#      - '*'
jobs:
  build:
    name: snapshot
    runs-on: ubuntu-latest
    steps:
      - run: pwd
        id: step1
      - run: -c ls
        id: step2
      - run: -c make snapshot
        id: step3
